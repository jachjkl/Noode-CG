# Windows 实时预览占用与重开保护（2026-09-15）

## 证据

run-20260915-160325.log 显示 16:34:44 更新 local-live-results.json.gz 时 os.replace 抛出 WinError 5，导致 local-select 退出。同期流程日志停在 19349/25022。失败发生在显示预览的写入，不是网络测试本身。

另一个可复现的生命周期问题：DashboardState 初始化无条件清理停止标记及临时结果；如果上一轮 local-select 尚在收尾，重新打开会删除仍被后台使用的状态。

## 修改

- 原子写入在 Windows 5/32/33 文件共享错误时进行有限重试，总退避最多 0.3 秒，不覆盖或截断旧文件。
- 初筛和竞赛的 UI 预览采用 best-effort 写入，失败日志提示后继续，下次刷新重试。正式交接和发布文件保持严格失败语义。
- 初始化时检查本应用的 local-select 进程；仍活动时接回监控并保留停止标记、实时结果。没有活动任务时维持原有新会话清理和按钮重置。

## 回归

增加实际 Windows 无删除共享句柄测试、模拟瞬时权限错误测试、预览与正式发布不同失败策略测试，以及后台活动时重开保护测试。旧的空闲重开清理与规则保留测试继续通过。

不从中断的半成品自动恢复发布，也不宣称所有异常中断可自动续跑。现有云端已发布结果不因这次修复被修改。

## 界面刷新性能

沿用 ui-ux-pro-max 的主线程预算、减少重排建议：表格无变化时跳过 DOM 更新，变化时只替换受影响的行；屏幕外卡片粒子暂停，隐藏页面暂停 UI 数据轮询，但浏览器存活心跳仍持续，避免后台误判为关闭。

Edge 无头浏览器使用 1000 行固定数据，连续刷新 20 次：修改前约 338ms、20 次整表重建；修改后约 27ms、0 次无效重建。覆盖实际数据变化、行增删/重排和屏幕外粒子暂停。这是渲染微基准，不代表实机鼠标交互帧率保证。

最终 Python 回归测试 163 项通过；浏览器性能及增量渲染检查通过。
